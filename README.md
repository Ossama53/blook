# blook
The project idea is to retrieve the disk image, read the image, reserve a block in memory, and then allocate the full size after the reservation. The full block is then divided into parts and read.
